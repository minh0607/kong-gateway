# SEHC AI Gateway — htpasswd template.
# Generate the real /DATA/kong/nginx/.htpasswd at first install via deploy.sh,
# which calls: htpasswd -bc nginx/.htpasswd <admin-username> <generated-password>
# Then create additional users from the admin GUI at http://<host>:8002/users/
